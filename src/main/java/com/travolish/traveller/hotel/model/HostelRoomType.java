package com.travolish.traveller.hotel.model;

/** Hostel-specific room types — selectable per room for HOSTEL category properties. */
public enum HostelRoomType {
    MIXED_DORM,
    FEMALE_DORM,
    MALE_DORM,
    PRIVATE_ROOM
}
